
dependencies {
    api project (":eventuate-common-in-memory-database")

    api "com.h2database:h2:1.3.166"
    api "org.springframework.boot:spring-boot-starter-jdbc:$springBootVersion"
}
tasks.withType(Test).configureEach {
    useJUnitPlatform()
}
